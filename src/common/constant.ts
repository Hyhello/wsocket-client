// 包名
export const SDK_NAMESPACE = '__NAMESPACE__';

// 版本
export const SDK_VERSION = '__VERSION__';

// 自定义code
export enum CloseEventCode {
	Normal = 1000, // Normal Closure Code
	HeartbaetTimeout = 4001 // HEARTBEAT FAIL
}
